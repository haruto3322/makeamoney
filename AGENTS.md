# AGENTS.md

このリポジトリで働くエージェントへの指示。Antigravity・Claude Code の両方が読む。

## このリポジトリは何か

参照動画から**カット表**と**動画生成プロンプト**を作る道具。さらに、そのプロンプトが
本当に狙った映像を再現できているかを検証するところまでを扱う。

詳しい設計は `docs/DESIGN.md`、使い方は `README.md`。連携の全体像は
`docs/ANTIGRAVITY.md`。

## 2 つのエージェントの分担

**リポジトリが受け渡しの場**になっている。互いに直接やり取りはせず、git を経由する。

| | Claude Code(クラウド) | Antigravity(Mac) |
|---|---|---|
| 動く場所 | Anthropic 側の環境。PC 不要 | Mac。起動しているときだけ |
| 動くとき | 5 時間ごとに自動 | 人が Agent Manager で走らせたとき |
| 得意なこと | 無人の定期実行、映像解析、判定 | ブラウザ操作、Gemini 3、並列実行 |
| 担当 | 合議 / カット表の解析 / 検証の判定 | **API の無い Google の画面を操作すること** |

この分担には理由がある。Google AI Pro は Gemini アプリと Flow を使う権利であって
API アクセスを含まない。つまり動画生成は**画面を操作するしかない**。
Antigravity のブラウザ制御は、そこを埋めるために使う。

## Antigravity が担当する仕事

### 1. 受信箱を見る

`work/queue/*.json` に未処理の依頼が入っている。1 ファイル = 1 本の生成依頼。

```jsonc
{
  "id": "gen_20260729-0930_cut003",
  "type": "video_generation",
  "service": "flow",              // labs.google/flow で生成する
  "prompt": "Close-up of ...",    // そのまま貼る。書き換えない
  "duration_sec": 2.6,            // 目標の尺
  "reference_frame": "out/.../frames/cut_003_f2.jpg",  // 参照。見比べる用
  "save_to": "out/.../verify/generated/cut_003.mp4",   // ここに保存する
  "cut_no": 3
}
```

### 2. 生成する

ブラウザで `service` の画面を開き、`prompt` をそのまま貼って生成する。
**プロンプトは書き換えない。** 検証しているのはそのプロンプト自体であって、
手を入れると何を測っているのか分からなくなる。

尺が選べる場合は `duration_sec` に最も近いものを選ぶ。

### 3. 結果を置いて commit する

1. できた動画を `save_to` のパスに保存する
2. 処理済みの依頼票を `work/queue/` から `work/done/` へ移す
3. `git add` して commit、push する

コミットメッセージは `Generate: <id>` の形にする。

生成に失敗したら、依頼票を `work/failed/` へ移し、同じ名前で `.md` を添えて
何が起きたかを書く(画面が変わっていた、上限に達した、など)。**黙って諦めない。**
次に読むのは人ではなくクラウド側のエージェントで、理由が書いていないと判断できない。

## やってはいけないこと

- **`council/` 配下を書き換えない。** 議題と役割定義は人が決める領域で、
  合議の結果もクラウド側が書く
- **`tools/` の解析ロジックを作り直さない。** カット検出・解析・比較はすでにあり、
  参照側と生成側で同じ処理を通すことが検証の前提になっている。別実装を足すと
  比較が成立しなくなる
- **`out/` の中の既存の解析結果を上書きしない。** 追加するのは
  `verify/generated/` の動画だけ
- **プロンプトを「良くしよう」としない。** 上に書いたとおり、検証対象そのもの

## 手を動かす前に読むもの

| やること | 読むもの |
|---|---|
| 全体像を掴む | `README.md` |
| なぜこの設計なのか | `docs/DESIGN.md` |
| 連携の仕組み | `docs/ANTIGRAVITY.md` |
| カット表の項目 | `.claude/skills/cutsheet/references/schema.md` |
| 検証の判定基準 | `tools/compare_cutsheets.py` の冒頭 |

## 書き方の約束

- コード中のコメントと、人が読む出力は**日本語**
- 生成 AI に渡すプロンプトは**英語**(そのほうが性能が出る)
- タイムコードや尺などの数値は、スクリプトが転記する。**エージェントが手で書き写さない**
