# 動画そのものは重いので追跡しない。
# キーフレームと解析結果は追跡する — これがクラウド側と手元の受け渡し面になる。
*.mp4
*.mov
*.webm

__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# セットアップのログ
.setup.log
.watch-state/
.watch.log
.watch.lock/
.council.log
.council.lock/

# 受け渡しの中身は使い捨て(依頼票そのものは追跡する)
work/**/*.mp4
work/**/*.mov
